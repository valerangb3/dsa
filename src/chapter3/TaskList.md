# Программные проекты

**3.1** В классе [`ArrayBub`](ArrayBub.java) реализуйте метод `bubbleSortWitTwoPointers()` так, чтобы в нем выполнялись двусторонние перемещения, иначе говоря, индекс inner сначала, как и прежде, переносит наибольший элемент слева направо, но затем он меняет направление и переносит наименьший элемент справа налево.

**3.2** В классе [`ArrayInsert`](ArrayInsert.java) реализовать метод с именем `median()`, возвращающий медиану массива.